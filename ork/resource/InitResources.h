namespace ork {

void InitResources();  //!< Explicit resource types initialization (the implicit one is working only in case program calls part of the library and so e.g. scenegraph samples was not working).

}  // ork
